import 'package:flutter/cupertino.dart';
import 'package:gupshop/widgets/passcode.dart';

class PasscodeLock extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Passcode(
    );
  }

  getIsValidCallBack(){

  }
}
